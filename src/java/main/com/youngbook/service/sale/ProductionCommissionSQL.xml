<?xml version="1.0" encoding="UTF-8"?>
<sql>
    <select-sql>
        <select id="listProductionCommission">
            SELECT
            *
            FROM
            sale_ProductionCommission p
            WHERE
            1 = 1
            AND  p.state = 0
        </select>
        </select-sql>
        </sql>
