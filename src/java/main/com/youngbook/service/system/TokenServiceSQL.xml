<?xml version="1.0" encoding="UTF-8"?>
<sql>
    <select-sql>
        <select id="checkAndDestroyToken">
select * from system_token where state=0
<if test="#{token} !=null">
and token=#{token}
</if>
<if test="#{bizId} !=null">
and bizId=#{bizId}
</if>
<if test="#{expiredTime} !=null">
and expiredTime&gt;=#{expiredTime}
</if>
and (UsedTime is null || UsedTime='')
        </select>
    </select-sql>
</sql>